<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= item.name %></title>
  </head>
  <body>
    <h1><%= item.name %></h1>
    <p>Year: <%= item.year %></p>
    <p>Price: $<%= item.price %></p>
    <p>Category: <%= item.category_name %></p>
    <a href="/categories/<%= item.category_id %>/items">Back to Items</a>
  </body>
</html>
