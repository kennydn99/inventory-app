<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/styles.css" />
    <title><%= item.name %></title>
  </head>
  <body>
    <%- include('../partials/navbar') %>
    <div class="container">
      <h2><%= item.name %></h2>
      <p>Year: <%= item.year %></p>
      <p>Price: $<%= item.price %></p>
      <p>Make: <%= item.category_name %></p>
      <a href="/items/<%= item.id %>/update">Update</a>
      <form
        action="/items/<%= item.id %>/delete"
        method="POST"
        onsubmit="return confirm('Are you sure you want to delete this model?');"
      >
        <button type="submit">Delete</button>
      </form>
      <a href="/categories/<%= item.category_id %>/items"
        >Back to <%= item.category_name %> Models</a
      >
    </div>
  </body>
</html>
